import { defaultLoader } from "~/shared/utils/loader";

export const loader = defaultLoader;

export default function Home() {
  return <div className="w-full h-full flex items-center justify-center">Web-version</div>
}