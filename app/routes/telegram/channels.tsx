import { defaultLoader } from "~/shared/utils/loader";

export const loader = defaultLoader;

export const Channels = () => {
  return <div className="w-full h-full flex items-center justify-center">Channels</div>
}